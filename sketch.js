var canvas;
var backgroundIMG, gameState = 0, playerCount, database, form, player, game;

function setup(){
    canvas = createCnavas(400,400);
    database = firebase.database();
}

function draw(){

}
